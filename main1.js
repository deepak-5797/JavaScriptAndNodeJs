//  map,filter and for loop 


//  for each loop = similar as java
// map if want to have particular value to be returned;

/*
      const arrayLet =  todos.map(function(todo){
            return todo.field ;
      })

      siliar filter will work it will filter out values similar syntax as map
*/

// ===  to compare

function addNumbers(num1,num2)
{
      console.log(num1+num2);
}

addNumbers();
